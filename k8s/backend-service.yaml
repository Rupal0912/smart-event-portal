apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  # ClusterIP is the default and makes the service only reachable from within the cluster.
  type: ClusterIP
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
